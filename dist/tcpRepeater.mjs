"use strict";let o=Deno.listen({hostname:"127.0.0.1",port:8092});for await(let e of o){console.debug("Connection accepted."),e.reader=e.readable.getReader(),e.writer=e.writable.getWriter();let r=!0;for(;r;)try{console.debug("Waiting to read...");let{value:t,done:a}=await e.reader.read();r=!a,t?(console.debug("Incoming data read."),e.writer.write(t)):console.debug("Incoming data ended.")}catch(t){console.debug(t),r=!1}}
